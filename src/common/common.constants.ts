export const BearerType = 'Bearer';

export enum Status {
  Active = 'Active',
  InActive = 'Inactive',
}

export enum Gender {
  Male = 'Male',
  Female = 'Female',
  Other = 'Other',
}

export enum Role {
  Admin = 'Admin',
  User = 'User',
}
